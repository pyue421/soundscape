let features;
function analyzeAudio() {
  if (audio && audio.isPlaying()) {
    features = Meyda.extract(['rms', 'spectralCentroid', 'energy', 'perceptualSpread'], audio.getPeaks ? audio.getPeaks() : audio);
    console.log(features);
  }
}